kagglehub
pandas
pillow
pyyaml
numpy
tqdm

# Training (classification).
# torch/torchvision: for a CUDA build, install from the matching index
# instead, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch
torchvision
timm

# Training (detection).
ultralytics
