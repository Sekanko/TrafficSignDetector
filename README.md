# TrafficSignDetector

Detektor + klasyfikator znaków drogowych (PyTorch → TensorFlow Lite, na telefon).

## Status

Obecny etap: **przygotowanie danych**. Planowane datasety: GTSRB, GTSDB,
Polish Traffic Signs Dataset (wszystkie z Kaggle).

## Setup

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
```

Konfiguracja Kaggle API: [docs/kaggle_setup.md](docs/kaggle_setup.md).
